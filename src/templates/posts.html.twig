{% extends 'base.html.twig' %}

{% block content %}
<ul>
    {% for post in slugs %}
    <li>
        <a href="/posts/{{ post.slug }}">{{ post.title }}</a>
    </li>
    {% endfor %}
</ul>
{% endblock %}