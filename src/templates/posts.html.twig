{% extends 'base.html.twig' %}

{% block content %}
<ul>
    {% for slug in slugs %}
    <li>
        <a href="../posts/{{ slug | url_encode }}">{{ slug }}</a>
    </li>
    {% endfor %}
</ul>
{% endblock %}